
"use client";

// This component is intentionally left blank.
// The previous functionality that blocked right-clicks and developer tools
// has been removed to improve user experience and align with browser-friendly practices.
export function SecurityProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
